# Auto-generated from Formula/get-ssid.rb.tmpl by `make package`. Do not edit this file directly.
class GetSsid < Formula
  desc "Print current Wi-Fi SSID on macOS without Location permission"
  homepage "https://github.com/fjh658/get-ssid"
  license "MIT"
  version "__VERSION__"

  tap_root = Pathname.new(__dir__).parent
  artifact = tap_root/"dist"/"get-ssid-__VERSION__-macos-universal.tar.gz"
  url "file://#{artifact}"
  sha256 "__SHA256__"

  depends_on :macos

  def install
    bin.install "get-ssid"
  end

  test do
    assert_match "USAGE:", shell_output("#{bin}/get-ssid --help")
  end
end
